#ifndef ENUMS_H
#define ENUMS_H

/**
 * @brief The MessageType enum is used to check/set the type of the message
 */
enum MessageType {
    BOAT_CONTROL,
    BOAT_DATA_PERSONNAL,
    BOAT_DATA_OTHERS,
    WEATHER_STATION,
};

#endif // ENUMS_H
